# Baseline 2026-04-26 — DRY chrome refactor complete

Snapshot of repo state after Phase 1 + Phase 2 of the chrome consolidation. Use this as the resume point in the next session.

---

## Session update 2026-05-03 (continued) — pre-merge smoke gates closed, 28/28 eval green, branch still NOT merged

HEAD: `32c0d3f`. Pushed. Branch `enter-v3.1-dev` is **9 commits ahead of main**. Main HEAD remains `7938970`. **Stay unmerged** until user signs off.

### What this commit shipped (`32c0d3f`)

| Smoke | Finding | Fix |
|---|---|---|
| 1 — voice direct | "tell me about voice ai at aionos" + "how does the 4 million calls platform work" both route to `lab/voice-ai-production` priority. Niche shift not over-suppressing. | No code change. |
| 2 — cache replay | Local `netlify dev` per-request worker model loses module-level dupCache between invocations. Verified on prod: `X-Cache: HIT` returns. Logic covered by `defense.smoke.mjs` in 111-pass suite. | No code change. Will work post-merge. |
| 3 — niche soft-warn | 0/3 runs of "what kind of pm is he" mentioned agent-first. Few-shot at line 273 led "Engineer-PM. He codes…" — overrode the new POSITIONING block because few-shots sit closer to the user message. | Rewrote the "Who is Agam?" + "What kind of PM is he?" few-shots to lead agent-first, engineer-PM as supporting beat. 3/3 runs now lead with agent-first. |
| 4 — belief surfacing | Belief queries the LLM answered without emitting `belief.<slug>` cards landed with `resume + lab + wiki/graph` defaults. Padder didn't know retrieval ran. | `padCardsToThree` now consumes `ctx.retrievedSlugs` (themes whose extract pulled + beliefs whose extract pulled) and prepends them to the family fillers. Two HARD RULES added to CARD RULES to bias the LLM toward emitting the matching card when retrieved-context block is present. 5/5 sample belief queries now surface the retrieved slug priority:true. |
| classifier gap | `belief-llm-as-primary-daily-tool` eval scenario flaked — query "why is llm his primary daily tool" missed `KEYWORD_TO_SLUG` (theme regex required "llm as primary tool" not "llm his primary daily tool"). | Added belief regex `/llm (his\|the) primary daily tool/i` → `belief.llm-as-primary-daily-tool`. |

Tests: 111 unit pass. eval-e2e: **28/28 PASS**.

### Niche shift coverage (continued)

POSITIONING block (line 179) was already agent-first. Few-shots at line 269 + 273 now match it. Plain "who is agam" + "what kind of pm is he" both lead with agent-first across 5/5 verified runs.

### Resume point for next session — **branch sign-off OR Phase E**

Two valid next moves, user picks:

1. **Sign off + merge to main.** Pre-merge smokes are clean, evals green, niche live. Merge `enter-v3.1-dev` → `main`. Netlify auto-deploys.
2. **Phase E or strategic carry-over.** Per CEO C5 deferral — niche shift currently lives only inside the Netlify function. `/`, `/resume`, OG taglines, LinkedIn headline still lead with engineer-PM hybrid or voice. Recruiter scanning `/resume` cold sees voice-AI prominence first. Cascade niche to surfaces outside `/enter`. Out of scope for v3.1, queue as own sprint.

### Pre-flight before resuming

1. `git status --short` — must be clean
2. `git log --oneline -3` — confirm HEAD is `32c0d3f`
3. `git rev-list --count main..enter-v3.1-dev` — should print 9
4. `npm test` — should print 111 passed
5. `npm run eval:e2e` — should print 28/28 PASS (only run after `netlify dev` is up on 8888)

### Explicitly deferred (do not chase)

- Design D1 (answer→cards spacing 24→32px) — cosmetic
- Design D3 (empty-state → first-turn crossfade) — cosmetic
- B6 (trace timing verb-key broadening) — minor
- Card alignment 32px drift — accepted status quo (memory: `feedback_card_alignment_gotcha.md`)
- Niche cascade to non-/enter surfaces — separate sprint per CEO C5

---

## Session update 2026-05-03 — fluffy-tinkering-crane plan landed (8 commits on enter-v3.1-dev), agent-first niche live, NOT merged to main

HEAD: `e90cee7`. Pushed. Working tree clean. Branch `enter-v3.1-dev` is **8 commits ahead of main** and **STAYS UNMERGED for now** (user explicit). Main HEAD remains `7938970`.

### What shipped this session (8 commits)

| Commit | Change |
|---|---|
| `6872547` | `scripts/perf/measure-card-alignment.mjs` Playwright tool — proves card-row positions across desktop + mobile viewports. Used to verify there was no alignment bug initially. |
| `0552cec` | Belief retrieval pipeline: build-wiki-extracts walks 19 belief pages (cap 600c each), new `beliefs-enum.mjs` (BELIEF_NAMESPACE='belief.', isBeliefSlug, BELIEF_SLUGS), themes-enum.filterValidSlugs accepts namespaced belief slugs + 'headline' marker, MAX_SYNTH_TOKENS 800→1000. |
| `866ad0a` | Card padder + headline routing + agent-first niche lock. card-meta gains validateLLMCards/padCardsToThree/pickPadFamily (F1: NO belief slugs in family defaults). classifier HEADLINE_RE preroute. SYSTEM_PROMPT_STABLE POSITIONING block leads with agent-first; CARD RULES adds HEADLINE row; buildDynamicContext gates on 'headline' marker. |
| `fcac4a5` | Belief retrieval wired live. wiki-retrieval.getBeliefExtract/getAllBeliefs. card-meta WIKI_BELIEFS registry. groqHandler partitions themes_likely[] via isBeliefSlug. classifier KEYWORD_TO_SLUG belief patterns + system-prompt belief slug list. |
| `c2b7a6e` | B5 retry trace fix — accepted retries always use `parsed.trace` (original) + append synthetic 'expanded' verb; failure branch leaves parsed unchanged. |
| `2c6fa97` | eval-e2e expansion: 5 base + 4 headline + 19 belief = 28 scenarios. F5 flex-card assertion (`cardsMinFlex/cardsMaxFlex` accept 1-3 with `padded` trace verb). scenario.history forwards into fetch body. |
| `e752c8b` | Eval gate fixes: server-stamps synthetic `pulled` trace verb when retrieval ran but LLM didn't emit a retrieval verb. DEFLECT_RES tightened to match (your\|his\|her\|their\|agam's). Loopback IP rate-limit bypass for local dev. **28/28 eval green.** |
| `e90cee7` | Card alignment investigation tool + status-quo doc. `scripts/perf/measure-2v3-cards.mjs` proved real bug: 3-card rows align at outer edge (110), 2-card rows align at text edge (142), 32px drift. Tested fixes both broke (drop-padding-left → text cutoff at user viewport; drop-negative-margin → 3-card row clips last card). User accepted status quo. Memory: `feedback_card_alignment_gotcha.md`. |

### Niche shift (live in /enter only)

System prompt POSITIONING now leads: *"Agam is an agent-first AI Product Manager. AI agents are the niche he builds for and thinks about — that is the headline."* Voice AI demoted to "production credential, NOT the headline answer." Live smoke confirmed: "what is his best work" → `wiki/agent-first` priority + `wiki/graph` + `lab` (no voice AI). Memory updated at `~/.claude/projects/D--AA-agamarora/memory/project_positioning_locked.md`.

### Resume point for next session — **finish pre-merge gates, hold main**

Pre-merge smoke (10 min, drive against `netlify dev` on 8888 — start it first):

1. **Voice direct query** — `npm run eval:e2e` only covers indirect; verify "tell me about voice ai at aionos" + "how does the 4 million calls platform work" still surface `lab/voice-ai-production` priority. Niche shift may have over-suppressed.
2. **Cache replay (B2)** — same query within 60s should replay 3 cards via `buildCacheReplayStream`. Test: send "what is his best work" twice, second response should `X-Cache: HIT` header + same 3 cards.
3. **niche-positioning soft-warn** — eval scenario `niche-positioning` PASSES but soft-warns answer doesn't include "agent". POSITIONING block not strongly followed on bare bio queries. Prompt-tune: maybe add explicit "lead with agent-first niche" instruction earlier in prompt or in SYSTEM_REMINDER.
4. **Belief card surfacing** — most belief queries surface `resume + lab + wiki/graph` (default family fillers) instead of the actual `belief.<slug>` card. LLM rarely emits belief cards even when retrieval ran. Either strengthen CARD RULES belief-card guidance OR extend padder to include `belief.<bare>` matching the retrieved beliefs as fillers.

After smoke: NOT merging to main. enter-v3.1-dev stays open until user decides.

### Strategic carry-over (separate sprint, not this branch)

- **CEO C5** — niche shift visibility. Agent-first currently lives only inside Netlify function. Recruiter scanning `/resume` sees voice-AI prominence first. Cascade niche to `/`, `/resume`, OG taglines, LinkedIn headline. Out of scope for v3.1; queue as own sprint.

### Explicitly deferred (do not chase)

- Design D1 (answer→cards spacing 24→32px) — cosmetic
- Design D3 (empty-state → first-turn crossfade) — cosmetic
- B6 (trace timing verb-key broadening) — minor, not cheap
- Card alignment 32px drift — accepted status quo per user choice (memory: `feedback_card_alignment_gotcha.md`)

### Pre-flight before resuming

1. `git status --short` — must be clean
2. `git log --oneline -3` — confirm HEAD is `e90cee7`
3. `git rev-list --count main..enter-v3.1-dev` — should print 8
4. `npm test` — should print 111 passed
5. Read `docs/plans/fluffy-tinkering-crane.md` for full plan + autoplan-fixes folded in
6. Start `netlify dev` on 8888 + (optional) gstack browser headed

---

## Session update 2026-05-02 — SEO audit close-out + OG overhaul + favicon + canonical fix + positioning lock

HEAD: `7938970`. Pushed. Working tree clean. **Resume point for next session: Phase D `/enter` v3 evals creation + testing.** Everything below is what shipped this session, in order.

### What shipped (6 commits)

| Commit | Change |
|---|---|
| `c1a0d60` | OG image overhaul: 3-bucket system at 2400×1256 JPEG. og-master.jpg (me-pages: /, /resume, /enter, /lab/voice-ai-production, /lab/product-shape — 182KB), og-wiki.jpg (37 wiki pages + /lab/second-brain — 158KB), og-general.jpg (/lab + /lab/ai-resume — 197KB). Killed blank unfurls on /resume + /enter (P0). Sized for iOS 16+ (1200×630 silently fails). All under 300KB WhatsApp budget. Deleted 4 legacy PNGs (lab.png, landing.png, second-brain.png, ai-resume.png). |
| `0d89698` | SEO audit sweep — closed 38-finding 2026-05-01 audit minus 3 intentional skips. P0-3 BreadcrumbList JSON-LD on wiki + lab + graph (build-script + hand pages). P1-1 Person sameAs +Medium +Calendly. P1-2 WebSite SearchAction. P1-13 ProfilePage @id ref. P1-4 meta desc trims (8 pages 188-368 chars → 140-148). P1-5 wiki title trims via build script (worst case 100→26 chars; 7 over-60-char titles fixed). P1-7 wiki OG (og-wiki.jpg). P1-3 Article schema image field. P1-12 og:locale en_US on 8 hand pages. P2 robots.txt (lowercase anthropic-ai, Meta-ExternalAgent, DuckDuckBot, Baiduspider). P2 LinkedIn URL canonicalize across 50 files (via chrome.mjs source). P1-9 ai.txt at root. Sitemap image refs swept from deleted PNGs. |
| `28e27de` | Favicon redesign: dropped trailing dot. "aa" mark across all 11 sizes (favicon.ico/svg/png + 16/32/96 raster + apple-touch + 192/512 PWA + 192/512 maskable). Patrick Hand woff2 → opentype.js → sharp → png-to-ico. The `.` reads as a stray pixel at favicon scale; full aa. mark stays in page footer. |
| `c7ffd8b` | **Hotfix:** removed netlify.toml `/resume/` → `/resume` and `/enter/` → `/enter` 301 redirects. Caused infinite loop because Netlify pretty-URLs default direction is the opposite (adds trailing slash, doesn't strip). Site went down for ~3 minutes with ERR_TOO_MANY_REDIRECTS. Memory saved at `~/.claude/projects/D--AA-agamarora/memory/feedback_netlify_pretty_urls.md`. |
| `f1f0134` | Canonical alignment: /resume + /enter canonical tags + og:url + sitemap + llms.txt + llms-full.txt + defense.mjs allowlist swapped from no-slash to with-slash to match Netlify pretty-URL default. |
| `7938970` | Followup: SearchAction urlTemplate also gets trailing slash. |

### SEO audit closure status

| Priority | Items | Closed | Skipped (intentional) |
|---|---|---|---|
| P0 | 5 | 5 (GSC + Bing verified out-of-band 2026-05-01; BreadcrumbList, /resume og, /enter og shipped today) | 0 |
| P1 | 13 | 11 | 2 (P1-3 datePublished/dateModified — timelessness preference; P1-8 llms-full.txt date bump — same; P1-10 twitter:creator — no active X handle) |
| P2 | 20 | ~12 (titles, descs, locale, image:type, robots, LinkedIn canon, ai.txt, etc) | ~8 (Yandex, IndexNow, FAQPage 2nd Q&A on 19 belief pages, Crawl-delay, /` H1 brand choice, sitemap lastmod per-page conflicts with timelessness) |

### Strategy-level decisions made this session (parked)

`/office-hours` session ran on SEO strategy. Locked these premises but PARKED full implementation pending holistic personal-branding pivot:

- SEO is NOT primarily for brand-vetting Google search ("Agam Arora") — that's downstream of upstream-not-yet-happening (resume reaching hiring managers at Anthropic/Stripe/Linear/Figma). Park branded SEO work.
- Real SEO targets = (a) cold thesis discovery in Google + (b) AI agent retrieval (Perplexity, ChatGPT browse, Claude with web).
- Canonical phrase to own = **"agent-first product"** / "agent-first AI products". `/wiki/agent-first` is the hub.
- **Positioning locked 2026-05-02:** Agam = elite B2B product manager with AI specialty. NOT generic "AI PM" (crowded, no edge). NOT "voice AI guy" (voice AI is evidence of range, not headline). Filter all future copy + content + SEO + brand through this frame.
- Honest read on SEO vs other levers: SEO + thought-leadership content is wrong primary lever for 6-12mo hiring outcome at top targets — that's an outbound + referral problem. SEO is right secondary lever for 3-year durable brand. **Personal-branding strategy session paused for holistic reconsideration.**

Memory: `~/.claude/projects/D--AA-agamarora/memory/project_positioning_locked.md`.

### Resume point for next session — **Phase D `/enter` v3 evals creation + testing**

Per `docs/plans/phase-d-dev-workflow.md` (LOCKED 2026-04-27), all `/enter` agent work — code AND eval scenarios — runs through:
- `netlify dev` on port 8888
- gstack browser headed
- Drive the real `/enter` UI for every change
- Function-only / isolated harness is NOT a sufficient signal

Plan canon: `docs/plans/enter-v3.md` + `docs/plans/second-brain-v1-next-session-plan.md` Tasks 14–22 (Phase D build) + Task 23 (Phase E launch with 23-scenario eval gate).

**Pre-flight before Phase D:**
1. `git status --short` (must be clean)
2. `git log --oneline -5` (confirm HEAD is `7938970` or descendant)
3. `git pull --ff-only origin main`
4. `npm run sync:chrome:check` (exits 0 = chrome system in sync)
5. Read `docs/plans/enter-v3.md` for current spec
6. Read `docs/plans/phase-d-dev-workflow.md` for the locked workflow
7. Start `netlify dev` + gstack browser before any eval-related code change

**Open SEO follow-ups (low-priority, queue-only, do NOT block evals):**
- llms.txt canonical facts add Medium + Calendly (P1, 2min)
- Short titles expand: /lab (17ch), /resume (20ch), /wiki/graph (12ch), /wiki/themes (14ch), /wiki/beliefs (14ch) — 15min
- JSON-LD `headline` em-dash normalize on wiki Article schema (build script, 5min)
- /wiki/graph priority 0.95 → 0.7 in sitemap (1min)

**Bigger track waiting:** holistic personal-branding strategy resume. Park until Phase D/E ships.

---

## Session update 2026-04-27 — italics fix + lab perf + QA pass

HEAD: `1ea596a`. Pushed. Working tree clean. Baseline below still describes the canonical chrome system (unchanged); this section covers what shipped today on top of it.

### What shipped

| Commit | Change |
|---|---|
| `dd4640a` | Italics regression fix — root cause: `<!-- chrome:css -->` markers placed inside `<style>` parsed as `chrome:css` ident:ident tokens, broke CSS cascade including `body { font-family: var(--sans) }`. Browser fell through to first-loaded `@font-face` (italic). Switched markers inside `<style>` to CSS-comment form `/* chrome:css */`. HTML-comment form `<!-- chrome:dom -->` etc still used in head + body where appropriate. `scripts/sync-chrome.mjs` updated to recognise both. |
| `18685f1` | `404.html` markers brought into line + `index.html` landing tagline split onto two lines via `<br>`. |
| `1ea596a` | Lab page perf: `assets/lab/second-brain-anim.mp4` re-encoded 1.69MB → 600KB (CRF 30, 24fps, libx264, 720×404). `<video>` on `/lab/` card switched to `data-src` + `preload="none"`, hydrated by an IntersectionObserver in `lab/index.html` (rootMargin 200px). PRFAQ hero on `/lab/second-brain/` keeps autoplay-on-load (motion expected on hero). `CLAUDE.md` stale FA-CDN note replaced; video conventions section now reflects actual encode params + IntersectionObserver pattern. `assets/lab/*.original.mp4` gitignored. |

### Perf numbers (cold-cache, 5-page baseline)

Captured with `npm run perf:bench` against prod. Local-only artefacts in `.gstack/benchmark-reports/baselines/baseline.json` (gitignored — re-run on a new machine).

| Page | TTFB | Total bytes | Notes |
|---|---|---|---|
| `/` | 260ms | 192KB | unchanged |
| `/wiki/` | 301ms | 58KB | unchanged |
| `/wiki/graph/` | 291ms | 127KB | unchanged |
| `/lab/` | 264ms | **150KB** | down from **1.84MB** (-92%) — video deferred, smaller when loaded |
| `/enter/` | 252ms | 62KB | unchanged |

`/lab/` first paint went from 1.84MB → 150KB. Two compounding wins:
- Re-encode (1.69MB → 600KB, -64%)
- Lazy-load (0KB until card scrolled into view)

### QA pass — `/qa` Quick tier

Health score **100/100**, 0 console errors across 6 pages. All chrome invariants verified post-refactor:

| Check | Pass |
|---|---|
| 5 SVG icons in header on every page (`#i-github`, `#i-linkedin`, `#i-youtube`, `#i-file-lines`, `#i-house`) | ✅ |
| `aa-mark` present on every page, tag = `<a>` (not `<div>`) | ✅ — confirms `/enter` aa-mark `<a>` bug fix held |
| `body` font-family = Satoshi, font-style = normal | ✅ — italics regression dead |
| `/lab/` lazy video: `data-src` + `preload="none"` + `autoplay=false` | ✅ |
| `/lab/second-brain/` hero video: eager `src` + `preload="metadata"` + `autoplay=true` | ✅ |
| HTTP 200 on all 6 pages | ✅ |

Report stored locally at `.gstack/qa-reports/qa-report-agamarora-com-2026-04-27.md` + screenshots in `.gstack/qa-reports/screenshots/`. Local-only, gitignored.

### New tooling (git-tracked)

| File | Purpose |
|---|---|
| `scripts/perf/bench.sh` | Cold-cache curl benchmark, 5 pages, writes JSON to `.gstack/benchmark-reports/`. Skips outbound `<a href>` destinations (uses `[[:space:]]src=` regex to also avoid false-counting `data-src`). |
| `scripts/perf/diff.sh` | Diffs latest run against `baselines/baseline.json`. |
| `scripts/perf/lab-breakdown.sh` | Per-asset byte breakdown for `/lab/`. |
| `package.json` scripts | `npm run perf:bench`, `npm run perf:diff`, `npm run perf:lab` |

### Next session resume — read this first

1. **Pre-flight** (same as before, but now also baseline-aware):
   ```bash
   git status --short                  # must be clean
   git log --oneline -5                 # confirm HEAD is 1ea596a (or descendant)
   git pull --ff-only origin main      # in case another machine pushed
   npm run sync:chrome:check            # exits 0 = chrome system in sync
   npm run perf:bench                   # captures fresh baseline (local-only)
   ```
2. **Then move to the queued work**, picking from this prioritised list (perf + testing finished, design + AEO + Phase D queue intact below):

   | Track | What | Est | Doc |
   |---|---|---|---|
   | **C-final** | `/design-review` on full wiki tree (33 pages) + `/review` on build pipeline + fix sweep + STATUS bump to CHECKPOINT 32 | 90-120 min | `docs/plans/second-brain-v1-next-session-plan.md` Tasks 1–3 |
   | **AEO/SEO gate** | 9 sub-tasks (Person schema → bot allowance) — BINDING before Phase D | 2-3 hr | `docs/aeo-seo-guidelines.md` + next-session-plan Tasks 4–13 |
   | **Phase D — `/enter` v3** | classifier + retrieval + SSE + abuse defense + 23-scenario eval | 4-6 hr (2-3 sessions) | `docs/plans/enter-v3.md` + next-session-plan Tasks 14–22 |
   | **Phase E — launch** | smoke test + 23/23 eval gate + thesis post + Medium/LinkedIn + 24h monitor | 2-4 hr | next-session-plan Task 23 |

   The `next-session-plan.md` task tracker rows 0 + 0a (site-nav promotion + graph CTAs) were SUPERSEDED this week — final state is the 5-icon SVG header (no text labels) + Wiki/Lab as landing CTAs (per commit `21b5a25`). Skip those rows; start at Task 1.

### Polish backlog (low priority, take when you want)

- Dedicated `/lab` OG image (still generic site preview).
- `buildGraphPage()` in `build-wiki.mjs` is 1,298 lines of inline JS template — split to `assets/graph.js` for editor comfort.
- `kg-themes-summary.mjs` is hand-authored — auto-generate from `build-kg.mjs` to prevent drift.
- Re-bench from a non-Windows machine to validate the bash extractor portability (current scripts work on Git Bash for Windows; Mac/Linux untested).

---

## What's live now

Every public page renders the same chrome (header, aa-mark, design tokens, font-face, reset, focus-visible) from a single source: `scripts/lib/chrome.mjs`.

**Header on every page** (5 entries, all FA solid SVG):

```
GitHub · LinkedIn · YouTube · Resume(file-lines) · Home
```

**Landing CTAs** (`/`):

```
Enter → · Wiki · Lab
```

**`/wiki/graph/` bottom-center CTAs:**

```
read the wiki → · talk to the agent →
```

---

## Architecture

```
scripts/
├── lib/
│   └── chrome.mjs          single source of truth — exports:
│                             SHARED_PRELOAD_HTML
│                             SHARED_FONTFACE_CSS
│                             SHARED_TOKENS_CSS
│                             SHARED_RESET_CSS
│                             SHARED_ICON_CSS
│                             SHARED_HEADER_CSS
│                             SHARED_AAMARK_CSS
│                             SHARED_FOCUS_CSS
│                             SHARED_CHROME_CSS  (bundled)
│                             SVG_SPRITE
│                             SHARED_HEADER_HTML
│                             SHARED_AAMARK_HTML
│                             AAMARK_SCRIPT
│
├── build-wiki.mjs          imports chrome.mjs; regenerates 36 wiki pages
├── build-kg.mjs            generates wiki/kg.json from ontology
└── sync-chrome.mjs         rewrites marker-bounded chrome blocks in 10 hand pages
```

**To change the header / sprite / aa-mark / chrome CSS site-wide:**

```bash
edit scripts/lib/chrome.mjs
npm run sync:chrome      # rewrites hand pages
npm run build            # regenerates wiki pages
git add . && git commit && git push
```

That's it. One edit, two commands. No more 9-file sweeps.

---

## Pages under sync

| Page | Markers in use | Notes |
|---|---|---|
| `index.html` | preload, css, dom, script | Landing |
| `404.html` | preload, css, dom, script | Custom centered body |
| `enter/index.html` | preload, css, sprite, header, aamark, script | Granular markers because keyboard backdrop sits between header and aa-mark |
| `resume/index.html` | preload, css, dom, script | |
| `lab/index.html` | preload, css, header, aamark, script | Sprite stays inline (extra page-specific symbols: i-brain, i-book-open, i-circle-play, i-video, i-microphone, i-gauge-high, i-download) |
| `lab/ai-resume/index.html` | preload, css, header, aamark, script | Same as above |
| `lab/product-shape/index.html` | preload, css, header, aamark, script | Same |
| `lab/second-brain/index.html` | preload, css, header, aamark, script | Same |
| `lab/voice-ai-production/index.html` | preload, css, header, aamark, script | Same |
| `wiki/index.html` | preload, css, header, aamark, script | Hand-edited (not built from script) |

**Wiki pages (36)** — generated by `scripts/build-wiki.mjs`, which imports the same `chrome.mjs` constants directly. No markers needed.

---

## What changed today

| Commit | Change |
|---|---|
| `4f3fd2b` | Task 0: nav text labels added (later reverted) |
| `8727225` | Task 0a: `/wiki/graph/` bottom-center entry CTAs |
| `21b5a25` | Header reverted to subtle 5-entry icon-bar (CV text variant — also reverted) |
| `496cf0f` | **CV text → file-lines SVG icon** (FA solid family). Sitemap dates bumped to 2026-04-26 |
| `90f247e` | DRY chrome refactor Phase 1: `lib/chrome.mjs` + `sync-chrome.mjs` + build-wiki.mjs imports. `/enter` aa-mark `<div>` → `<a>` bug fix. focus-visible + Satoshi preload added to wiki |
| _next_ | DRY chrome refactor Phase 2: marker installation on 10 hand pages, sync run, all pages now use canonical chrome |

---

## What's left — big items only

### 1. Phase D — `/enter` v3 (LLM plumbing)

Per `docs/plans/enter-v3.md` and `docs/plans/second-brain-v1.md` §6+§7:

- Rewrite `netlify/functions/groqHandler.mjs`:
  - Classifier (Groq 8B, 200-token JSON output): intent, confidence, themes_likely
  - Tier routing: lookup/deflect/bio → 8B; synthesis → DeepSeek/Mixtral; fallback chain
  - System prompt v3 with stable cache breakpoint (5 min Anthropic cache TTL)
  - Wiki retrieval pipeline (60s in-memory cache, LRU 50 entries)
  - SSE streaming events: trace / token / card / done
  - Abuse defense Tier 0 (UA gate + input validation + dup cache + injection filter)
  - Abuse defense Tier 1 (Upstash per-IP bucket + low-effort gate + multi-key Groq rotation)
  - Abuse defense Tier 2 (spend caps: $3/day, $30/month, $0.05/query)
- Update `enter/index.html` UI to consume new SSE events (trace stagger, card render strip)
- Eval harness: 23 pinned scenarios from `enter-v3-scenarios-v2.md` — 23/23 required to ship
- Multi-key Groq rotation (4 keys round-robin, 60s cool-down on 429)

Estimate: ~4-6 hours focused work. Spread across 2-3 sessions.

### 2. AEO/SEO gate (BINDING, blocks Phase D launch)

Per `docs/aeo-seo-guidelines.md`:

- AEO-1 Person schema enrichment on `/`
- AEO-2 robots meta directive audit
- AEO-3 Q&A overlay on `/wiki/voice/` + `/wiki/quotes/`
- AEO-4 BLUF executive summary on Lab PRFAQs
- AEO-5 image alt audit
- AEO-6 `<pre><code>` wrap audit
- AEO-7 TTFB benchmark + record
- AEO-8 bot allowance verification (GPTBot, ClaudeBot, PerplexityBot, etc.)
- AEO-9 post-AEO regression review
- Crawl audit checklist (Part 5)

Estimate: ~2-3 hours.

### 3. Phase C — design / review pass

- `/design-review` on full wiki tree (33 pages) — gstack skill
- `/review` on build pipeline — gstack skill
- Fix sweep
- STATUS bump to CHECKPOINT 32 = Phase C COMPLETE

Estimate: 90-120 min.

### 4. Phase E — launch

- Smoke test on production /enter v3 (5 queries)
- Run 23/23 eval against production (gate)
- Push thesis post at `/writing/second-brain-live/` (separate writing session)
- Publish on Medium + cross-post LinkedIn
- Monitor first 24h

Estimate: 2-4 hours.

### 5. Polish backlog (low priority, take when you want)

- `/lab` OG image is still the generic site preview. Make a dedicated one.
- Font Awesome 6.5.0 CDN load — confirmed unused now (CLAUDE.md note is stale; actual icons are inline SVG sprites). Update the doc.
- `buildGraphPage()` in `build-wiki.mjs` is 1,298 lines of inline JS template inside the build script. Consider splitting graph JS to `assets/graph.js`. Mainly editor comfort, no perf change.
- `kg-themes-summary.mjs` is hand-authored, not generated. Auto-generate it from `build-kg.mjs` to prevent drift.

---

## How to verify the chrome system is working

```bash
# 1. Sync should be a no-op (everything in sync)
npm run sync:chrome:check    # exits 0 if all 10 pages match canonical

# 2. Build should produce 36 wiki pages
npm run build                # builds kg.json + 36 wiki pages

# 3. Local serve to eyeball
npx serve . -p 5173
```

CI guard suggestion: add `npm run sync:chrome:check` to a pre-commit hook or Netlify build to catch drift.

---

## Resume context for next session (paste verbatim)

```
agamarora.com is at the post-DRY-refactor baseline (commit <fill in>). All 9 hand
pages + wiki/index.html now sync chrome from scripts/lib/chrome.mjs via
scripts/sync-chrome.mjs. Build-wiki.mjs imports the same module, so 36 wiki
pages stay aligned automatically.

Open work, in order:
  1. Phase C close: /design-review pass + /review pass on wiki + build
  2. AEO/SEO gate (binding before Phase D)
  3. Phase D: /enter v3 with classifier, retrieval, SSE, abuse defense, eval
  4. Phase E: launch (thesis post + 23/23 eval gate)

See docs/plans/BASELINE-2026-04-26.md for the full plan + estimates.
See docs/plans/second-brain-v1-next-session-plan.md for the per-task breakdown.
See CLAUDE.md for project rules (subagent model policy, conventions, stack).
```
