#environment variables
.env

#Node modules
node_modules

# Local Netlify folder
.netlify
.gstack/

# Local Claude Code / session artifacts (never committed)
.claude/
.context/

# /design-review session artifacts (screenshots, audit reports — local only)
.gstack-design-audit/

# OS
.DS_Store
Thumbs.db

# Obsidian local config (Phase A authoring tool)
.obsidian/

# Re-encode source — kept locally, not served
assets/lab/*.original.mp4

# Playwright artifacts from perf scripts (regenerated on demand, not committed)
scripts/perf/*.png
